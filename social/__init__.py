
""" In this app we can see skills for example as context ´processors'. Also check the 'settings' script since there
    were modifications to that script. We added a new function to the 'context processors' list so we can access
    to that function from every possible template """
